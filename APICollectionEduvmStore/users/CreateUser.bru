meta {
  name: CreateUser
  type: http
  seq: 3
}

post {
  url: http://localhost:8000/api/users/
  body: json
  auth: none
}

body:json {
  {
    "role" : "{{new_role_id}}"
  }
}

vars:post-response {
  new_user_id: res("id")
}
